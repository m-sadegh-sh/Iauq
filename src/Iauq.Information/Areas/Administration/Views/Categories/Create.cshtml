@using Iauq.Web.Mvc.UI
@model Iauq.Core.Domain.Category

@{
    ViewBag.Title = "پنل مدیریت - دستهبندیها - ثبت دستهبندی جدید";
    PagePartManager.Current.AddBreadcrumb(Html.ActionLink("پنل مدیریت", "Default", "Administration"));
    PagePartManager.Current.AddBreadcrumb(Html.ActionLink("دستهبندیها", "List", "Categories", new { page = 1 }, null));
    PagePartManager.Current.AddBreadcrumb(Html.ActionLink("ثبت دستهبندی جدید", "Create", "Categories"));
}

<div class="rounded-box newest row-fluid">
    <div class="inside">
        <h1>@ViewBag.Title
        </h1>
        <hr />
        @using (Html.BeginForm("Create", "Categories", FormMethod.Post, new {@class = "form-horizontal"}))
        {
            @Html.ValidationSummary(true)

            <fieldset>
                @{ Html.RenderPartial("_NewOrUpdate", Model); }
                <div class="form-actions">
                    <button type="submit" class="btn btn-success">ثبت</button>
                    @Html.ActionLink("برگشت", "List", new{page=1}, new {@class = "btn"})
                </div>
            </fieldset>
        }
    </div>
</div>